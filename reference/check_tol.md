# Validate a Tolerance

Every criterion constructor takes a tolerance, and every one of them
should reject the same nonsense in the same words.

## Usage

``` r
check_tol(tol)
```

## Arguments

- tol:

  The value supplied.

## Value

Invisibly `TRUE`; raises an error otherwise.
