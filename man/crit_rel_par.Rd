% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criterion.R
\name{crit_rel_par}
\alias{crit_rel_par}
\title{Stop When the Parameters Stop Moving (Relative)}
\usage{
crit_rel_par(tol = 1e-08)
}
\arguments{
\item{tol}{Numeric tolerance, a single positive number. Defaults to
\code{1e-8}.}
}
\value{
An S7 object of class \link{CritRelPar}, inheriting from
\code{\link[=criterion]{criterion()}}.
}
\description{
The rule
\eqn{\max_j \lvert x_j^{new} - x_j^{old}\rvert / (\lvert x_j^{old}\rvert + \texttt{tol}) < \texttt{tol}}.
}
\details{
The \code{+ tol} in each denominator is a floor, as in \code{\link[=crit_rel_obj]{crit_rel_obj()}}: a
coordinate sitting at zero would otherwise be divided by nothing. The same
number therefore serves as the floor and as the threshold.
}
\examples{
crit_rel_par()

# The same relative change at two very different scales.
c(large = crit_met(crit_rel_par(1e-6), list(x_new = 1000,
                                            x_old = 1000.0001)),
  small = crit_met(crit_rel_par(1e-6), list(x_new = 1e-3,
                                            x_old = 1e-3 + 1e-10)))

}
\seealso{
\code{\link[=crit_abs_par]{crit_abs_par()}} for the version in the parameters' own units.
}
