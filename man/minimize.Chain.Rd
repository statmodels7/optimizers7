% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chain.R
\name{minimize.Chain}
\alias{minimize.Chain}
\title{Minimize by a Sequence of Optimizers}
\arguments{
\item{optimizer}{A \code{Chain} object.}

\item{fn, par, gr, he, lower, upper, ...}{As in \code{\link[=minimize]{minimize()}}. Passed to every
stage as given, with \code{par} replaced by the previous stage's answer.}
}
\value{
An \code{\link[=optimizer_result]{optimizer_result()}} carrying the last stage's point, value,
gradient, iteration count and verdict, the summed evaluation counts, the
stacked trace and the elapsed time of the whole chain. \code{seed} is the
\strong{first} stage's, that being the state a repeat of the chain has to
start from.
}
\description{
Runs the stages in order, handing each the point the previous one reached,
and assembles one result from them. The objective, its derivatives and the
bounds are passed to every stage unchanged; only the starting point moves.
}
\details{
Two things happen before the first stage runs. Every stage's stopping rule
is put to \code{\link[=check_criterion]{check_criterion()}}, so a chain whose last stage cannot evaluate
its own rule fails without spending the earlier ones. And the
gradient-consistency check is switched off for the duration, the generic
having already made it once at the caller's \code{par}; without that, the same
warning would print once per stage.
}
\keyword{internal}
