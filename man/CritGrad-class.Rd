% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criterion.R
\name{CritGrad-class}
\alias{CritGrad-class}
\alias{CritGrad}
\alias{crit_met.CritGrad}
\alias{crit_needs.CritGrad}
\title{S7 Class for the Gradient Criterion}
\usage{
CritGrad(label = character(0), tol = integer(0), norm = character(0))
}
\arguments{
\item{tol}{The tolerance, a single positive number.}

\item{norm}{Either \code{"max"} or \code{"2"}.}
}
\value{
An S7 object of class \code{CritGrad} inheriting from \code{\link[=criterion]{criterion()}},
carrying \code{label}, \code{tol} and \code{norm}.
}
\description{
The rule \code{\link[=crit_grad]{crit_grad()}} builds, and the two methods it implements. It
reads \code{state$gradient}, takes its max-norm or its 2-norm, and fires when
that falls below \code{tol}. It declares \code{"gradient"} through \code{\link[=crit_needs]{crit_needs()}},
so a derivative-free optimizer refuses it when the run starts.
}
\details{
\code{crit_met()} returns \code{FALSE} when the gradient is \code{NULL}, empty or carries
an \code{NA}, so a state that has not yet filled it in never accidentally
satisfies the rule. Which norm is used is the \code{norm} property: on a
gradient of \eqn{(3, 4)}, \code{crit_grad(4.5, "max")} fires and
\code{crit_grad(4.5, "2")} does not.
}
\examples{
st <- list(iter = 1, f_new = 1, f_old = 2, x_new = 1, x_old = 0,
           gradient = c(3, 4))
crit_needs(crit_grad())
c(max = crit_met(crit_grad(4.5, "max"), st),
  two = crit_met(crit_grad(4.5, "2"), st))

# A gradient that is not there is not a small gradient.
crit_met(crit_grad(), list(f_new = 1, gradient = NULL))

}
\seealso{
\code{\link[=crit_grad]{crit_grad()}} for the constructor and the tolerance it can
attain, \code{\link[=crit_stationary]{crit_stationary()}} for what a derivative-free method reads.
}
\keyword{internal}
